>>=|:::Promise:::|=>>
    -> Promises were introduced in ES6 (2015) to solve callback hell and provide a cleaner way to handle asynchronous operations. A promise represents a future value that can be fulfilled or rejected.

    -> A Promise is an object that represents a future value.

    -> It promises that something will happen later — either success or failure.


    🕰️ When did Promise come?
        -> Year: 2015
        -> Version: ES6 (ECMAScript 2015)
    
    🔁 Promise States (VERY IMPORTANT)
        A Promise has 3 states:
            1️⃣ Pending
                ->Initial state
                ->Task is running

            2️⃣ Fulfilled (Resolved)
                ->Task completed successfully
                ->Result available

            3️⃣ Rejected
                ->Task failed
                ->Error occurred
    
    🧱 How a Promise is CREATED
        let promise = new Promise((resolve, reject) => {
            // async task
        });
        ->resolve() → success
        ->reject() → failure

    🔗 Promise Chaining (Key Feature)
        getUser()
            .then(user => getProfile(user))
            .then(profile => getPosts(profile))
            .then(posts => {
                console.log(posts);
            })
            .catch(err => {
                console.log(err);
            });

            ✔ No nesting
            ✔ Clean flow
            ✔ One error handler
    
    ⚙️ Important Promise Methods
        1️⃣ Promise.resolve()
            Promise.resolve("Done").then(console.log);
        
        2️⃣ Promise.reject()
            Promise.reject("Error").catch(console.log);

        3️⃣ Promise.all() -> ❌ If one fails, all fail
            Promise.all([p1, p2, p3])
                .then(results => console.log(results))
                .catch(err => console.log(err));
            
            Example:-
                let p1 = Promise.resolve("A");
                let p2 = Promise.resolve("B");
                let p3 = Promise.resolve("C");

                Promise.all([p1, p2, p3])
                    .then(result => {
                        console.log(result);
                    })
                    .catch(err => console.log(err));
                🖨️ Output:
                    ["A", "B", "C"]


        4️⃣ Promise.allSettled() -> ✔ Gives result of all (success + failure)
            Promise.allSettled([p1, p2]);
        
         Example:-
            let p1 = Promise.resolve("Success");
            let p2 = Promise.reject("Failed");
            Promise.allSettled([p1, p2])
                .then(result => {
                    console.log(result);
                });
            🖨️ Output:
                [
                    { status: "fulfilled", value: "Success" },
                    { status: "rejected", reason: "Failed" }
                ]


        5️⃣ Promise.race() -> First finished promise wins
            Promise.race([p1, p2]);
        Example:-
            let p1 = new Promise(res => setTimeout(() => res("Fast"), 1000));
            let p2 = new Promise(res => setTimeout(() => res("Slow"), 3000));
            Promise.race([p1, p2])
                .then(result => {
                    console.log(result);
                });
            🖨️ Output:
                Fast

        
        6️⃣ Promise.any() -> ✔ First successful promise wins
            Promise.any([p1, p2]);
        Example:-
            let p1 = Promise.reject("Error 1");
            let p2 = Promise.resolve("Success");
            let p3 = Promise.resolve("Another success");

            Promise.any([p1, p2, p3])
                .then(result => {
                    console.log(result);
                });
            🖨️ Output:
                Success

        ❌ If all are rejected:
            Promise.any([p1, Promise.reject("Error 2")])
                .catch(err => console.log(err));
            🖨️ Output:
                AggregateError: All promises were rejected



    ⚖️ Callback vs Promise
        +------------------------------------------+
        | Feature        | Callback  |  Promise    |
        |----------------|-----------|-------------|
        | Readability    | ❌ Bad    | ✅ Good     |
        | Nesting        | ❌ Yes    | ❌ No       |
        | Error handling | ❌ Hard   | ✅ Easy     |
        | Modern JS      | ❌ Old    | ✅ Standard |
        +-------------------------------------------+
    
    🧠 Visual Flow (Text Diagram)
        ┌──────────────────────────────────────────┐
        │              JavaScript Engine           │
        │                                          │
        │   ┌───────────────┐                      │
        │   │   Call Stack  │  ← Executes JS code  │
        │   └───────────────┘                      │
        │            ▲                             │
        │            │                             │
        │       ┌─────────┐                        │
        │       │ Event   │  ← Traffic Controller  │
        │       │ Loop    │                        │
        │       └─────────┘                        │
        │            ▲                             │
        │            │                             │
        │   ┌──────────────────┐                   │
        │   │ Microtask Queue  │  ← HIGH priority  │
        │   │ (Promises)       │                   │
        │   └──────────────────┘                   │
        │            ▲                             │
        │            │                             │
        │   ┌──────────────────┐                   │
        │   │ Callback Queue   │  ← LOW priority   │
        │   │ (Timers, Events) │                   │
        │   └──────────────────┘                   │
        │            ▲                             │
        │            │                             │
        │   ┌──────────────────┐                   │
        │   │   Web APIs       │  ← Browser APIs   │
        │   │ (fetch, timer)   │                   │
        │   └──────────────────┘                   │
        └──────────────────────────────────────────┘

    ⚖️ ONE LOOK TABLE (VERY IMPORTANT)
        ┌─────────────────────────────────────────────┐
        │                JavaScript Runtime           │
        └─────────────────────────────────────────────┘
                │
                ▼
        ┌─────────────────────────────────────────────┐
        │                 CALL STACK                  │
        │  (Synchronous / Immediate Execution)        │
        ├─────────────────────────────────────────────┤
        │  • console.log()                            │
        │  • function calls                           │
        │  • normal JS statements                     │
        └─────────────────────────────────────────────┘
                │
                ▼
        (Async tasks handed to browser)
        ┌─────────────────────────────────────────────┐
        │                   WEB APIs                  │
        │  (Browser handles async work here)          │
        ├─────────────────────────────────────────────┤
        │  • fetch()  → network request               │
        │  • setTimeout() → timer                     │
        │  • DOM events → click, keypress             │
        └─────────────────────────────────────────────┘
                │
                ▼
        (When async work finishes)
        ┌─────────────────────────────────────────────┐
        │             MICROTASK QUEUE (HIGH)          │
        │        (Executed BEFORE Callback Queue)     │
        ├─────────────────────────────────────────────┤
        │  • Promise.then()                           │
        │  • Promise.catch()                          │
        │  • Promise.finally()                        │
        │  • async/await (code after await)           │
        │  • fetch() response handling                │
        └─────────────────────────────────────────────┘
                │
                ▼
        (Only when Microtask Queue is EMPTY)
        ┌─────────────────────────────────────────────┐
        │            CALLBACK QUEUE (LOW)             │
        │        (Timers & Events – Macrotasks)       │
        ├─────────────────────────────────────────────┤
        │  • setTimeout() callback                    │
        │  • setInterval() callback                   │
        │  • DOM event handlers                       │
        └─────────────────────────────────────────────┘
                │
                ▼
        ┌─────────────────────────────────────────────┐
        │                EVENT LOOP                   │
        │  (Moves tasks to Call Stack in this order)  │
        ├─────────────────────────────────────────────┤
        │  1️⃣ Call Stack (sync code)                  │
        │  2️⃣ Microtask Queue (ALL tasks)             │
        │  3️⃣ Callback Queue (ONE task)               │
        │  🔁 Repeat                                  │
        └─────────────────────────────────────────────┘


🌐 JAVASCRIPT RUNTIME – FULL MERGED TEXT DIAGRAM


┌─────────────────────────────────────────────────────────────────────┐
│                           JAVASCRIPT RUNTIME                        │
│                                                                     │
│  ┌───────────────────────────┐     ┌─────────────────────────────┐  │
│  │        MEMORY HEAP        │     │          CALL STACK         │  │
│  │  (Objects, variables)     │     │  ┌────────────────────────┐ │  │
│  │                           │     │  │   fn()                 │ │  │
│  │                           │     │  │   fn()                 │ │  │
│  │                           │     │  │   fn()                 │ │  │
│  │                           │     │  │   Global Execution     │ │  │
│  └───────────────────────────┘     │  └────────────────────────┘ │  │
│                                    │              ▲              │  │
│                                    │              │              │  │
│                                    └───── call ───┘              │  │
└─────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ (async task handed over)
                                      ▼
┌─────────────────────────────────────────────────────────────────────┐
│                               WEB APIs                              │
│                                                                     │
│  ┌──────────────┐   ┌──────────────┐   ┌────────────────────────┐   │
│  │   DOM API    │   │  setTimeout  │   │        fetch()         │   │
│  └──────────────┘   └──────────────┘   └────────────────────────┘   │
│                                                                     │
│        (Browser handles async work here)                            │
│                                                                     │
│         ┌───────────────────────────────┐                           │
│         │   Register Callback (CB)      │                           │
│         └───────────────────────────────┘                           │
└─────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ (when async work finishes)
                                      ▼

          🔴 HIGH PRIORITY (PROMISES FIRST)
┌─────────────────────────────────────────────────────────────────────┐
│                         MICROTASK QUEUE                             │
│                    (High Priority Queue)                            │
│                                                                     │
│   [ Promise.then() ]   [ Promise.catch() ]                          │
│   [ Promise.finally()] [ async/await after await ]                 │
│   [ fetch().then() ]                                                │
│                                                                     │
│   MUST be completely EMPTY before Callback Queue runs               │
└─────────────────────────────────────────────────────────────────────┘
                                      │
                                      │ (moved by Event Loop)
                                      ▼

          🔵 LOW PRIORITY (TIMERS & EVENTS)
┌─────────────────────────────────────────────────────────────────────┐
│                         CALLBACK QUEUE                              │
│                    (Task / Macrotask Queue)                         │
│                                                                     │
│   [ setTimeout callback ]   [ setInterval callback ]                │
│   [ DOM Event callbacks ]                                           │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
                                      │
                                      │
                                      ▼
┌─────────────────────────────────────────────────────────────────────┐
│                             EVENT LOOP                              │
├─────────────────────────────────────────────────────────────────────┤
│   1️⃣ Check if CALL STACK is empty                                  │
│   2️⃣ Move ALL MICROTASKS to CALL STACK                             │
│   3️⃣ If Microtask Queue empty →                                    │
│     Move ONE CALLBACK QUEUE task                                    │
│   4️⃣ Repeat forever 🔁                                             │
└─────────────────────────────────────────────────────────────────────┘
                                      │
                                      ▼
                            (Back to CALL STACK)



